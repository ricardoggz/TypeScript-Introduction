/*Declarar una tupla en TypeScript:*/

//similares a un arreglo, pero con la cualidad de poder 
//controlar los tipos de datos y la longitud del arrelo

const exampleTuple : [string, number, boolean] = ["ricardo", 21, false];